\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{flushend}[2026/04/29 local fallback for report compilation]

% The original course template uses flushend only to balance the final two
% columns. This fallback keeps compilation reproducible on systems where the
% package is not installed; it intentionally does not alter output layout.

\endinput
